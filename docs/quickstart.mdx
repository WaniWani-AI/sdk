---
title: Quickstart
description: "Get a working MCP server with analytics in under 5 minutes."
---

## Choose your setup

<CardGroup cols={3}>
  <Card title="Managed project" icon="cloud" href="#option-a-managed-project">
    WaniWani provisions a repo, deploys to Vercel, and auto-tunnels local dev. Best for **new MCP servers** with zero infrastructure to manage.
  </Card>
  <Card title="Template" icon="rocket" href="#option-b-start-from-template">
    Clone a ready-to-go repo with the SDK pre-configured. Best for **new projects** you want to host yourself.
  </Card>
  <Card title="Existing project" icon="server" href="#option-c-existing-project">
    Add the SDK to your own MCP server. Best for **existing MCP servers** where you want to add tracking, flows, or knowledge base.
  </Card>
</CardGroup>

---

## Option A: Managed project

WaniWani provisions a GitHub repo, deploys to Vercel, and auto-tunnels local dev so ChatGPT and Claude can reach your server instantly.

<Steps>
  <Step title="Create a managed project">
    1. Open [app.waniwani.ai/mcp](https://app.waniwani.ai/mcp) and sign in.
    2. Click **Create project** and choose **Managed** ("We host your MCP").
    3. WaniWani creates a GitHub repo and Vercel project for you. Two environments are provisioned automatically: **Staging** and **Production**.
    4. Clone the repo locally.
  </Step>

  <Step title="Install and run">
    <Note>Managed projects use **Bun** as the package manager.</Note>

    ```bash
    bun install
    bun dev
    ```

    `bun dev` starts a Cloudflare tunnel automatically. Your local server is now reachable from ChatGPT and Claude. No ngrok, no port forwarding.
  </Step>

  <Step title="Open the playground">
    The dev server prints a tunnel URL. Open it in your browser to access the built-in chat playground, or connect from ChatGPT/Claude using the tunnel URL + `/mcp`.
  </Step>
</Steps>

---

## Option B: Start from template

The [MCP Distribution Template](https://github.com/WaniWani-AI/mcp-distribution-template) gives you a production-ready MCP server with the SDK, event tracking, flows, and widgets already wired up.

<Steps>
  <Step title="Create your repo">
    Click **"Use this template"** on [GitHub](https://github.com/WaniWani-AI/mcp-distribution-template) to create a new repository from the template, or clone it directly:

    ```bash
    git clone https://github.com/WaniWani-AI/mcp-distribution-template.git my-mcp-server
    cd my-mcp-server
    ```
  </Step>

  <Step title="Install and configure">
    ```bash
    bun install
    cp .env.example .env
    ```

    Add your API key to `.env`:

    ```bash .env
    WANIWANI_API_KEY=wwk_...
    ```

    Get a key from [app.waniwani.ai](https://app.waniwani.ai). Create an **External** project and copy the `wwk_...` key.
  </Step>

  <Step title="Run">
    ```bash
    bun dev
    ```

    The template includes a reference flow you can try immediately. If you have the [agent skill](/guides/skills) installed, run `/waniwani-sdk initialize` to scaffold a flow tailored to your business.
  </Step>
</Steps>

---

## Option C: Existing project

You already have an MCP server and want to add WaniWani to it.

<Steps>
  <Step title="Install the SDK">
    <CodeGroup>
      ```bash bun
      bun add @waniwani/sdk
      ```

      ```bash pnpm
      pnpm add @waniwani/sdk
      ```

      ```bash npm
      npm install @waniwani/sdk
      ```
    </CodeGroup>
  </Step>

  <Step title="Create an external project and get an API key">
    1. Open [app.waniwani.ai/mcp](https://app.waniwani.ai/mcp) and sign in.
    2. Click **Create project** and choose **External** ("You host your MCP").
    3. Set your server URL for the production environment.
    4. Copy the API key (`wwk_...`). It is shown only once.

    ```bash .env
    WANIWANI_API_KEY=wwk_...
    ```
  </Step>

  <Step title="Wrap your server">
    Call `withWaniwani(server)` once. Every tool call now emits a `tool.called` event to your dashboard.

    ```ts server/src/index.ts
    import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
    import { StreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/streamableHttp.js";
    import { withWaniwani } from "@waniwani/sdk/mcp";
    import "dotenv/config";

    const server = new McpServer({ name: "my-mcp", version: "0.0.1" });

    server.registerTool(/* ... */);

    withWaniwani(server);

    const transport = new StreamableHTTPServerTransport({ sessionIdGenerator: undefined });
    await server.connect(transport);
    ```
  </Step>

  <Step title="Verify">
    Trigger any tool call, then check the [WaniWani dashboard](https://app.waniwani.ai). The event should appear within seconds.

    If nothing shows up, confirm `WANIWANI_API_KEY` is set and look for `[waniwani]` lines in your server logs.
  </Step>
</Steps>

## What's next

<CardGroup cols={2}>
  <Card title="Event tracking" icon="chart-line" href="/tracking/overview">
    Custom events, sessions, and user identification.
  </Card>
  <Card title="Build a flow" icon="diagram-project" href="/flows/overview">
    Multi-turn stateful conversations.
  </Card>
  <Card title="Knowledge base" icon="book" href="/knowledge-base/overview">
    Ingest files and search with embeddings.
  </Card>
  <Card title="withWaniwani reference" icon="wand-magic-sparkles" href="/configuration/wrap-server">
    All options for the server wrapper.
  </Card>
  <Card title="Tunnel" icon="globe" href="/guides/tunnel">
    Expose your local server to ChatGPT and Claude.
  </Card>
</CardGroup>
